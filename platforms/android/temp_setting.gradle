android {
            defaultConfig {
                applicationId = "org.nativescript.picturebank"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }